"use client";
import { useEffect, useState } from "react";
import { supabase } from "../../lib/supabase";
type Site={id:string;site_name:string}; type Task={id:string;plot_number:string|null;task_name:string|null;trade:string|null;start_date:string|null;end_date:string|null;status:string|null};
function statusStyle(status:string|null){switch(status){case"Complete":return{background:"#d4edda",color:"#155724"};case"In Progress":return{background:"#fff3cd",color:"#856404"};case"Delayed":return{background:"#f8d7da",color:"#721c24"};case"At Risk":return{background:"#ffe5b4",color:"#8a5a00"};default:return{background:"#e9f3ff",color:"#0f2747"}}}
export default function ProgrammeVisual(){const[role,setRole]=useState("");const[userTrade,setUserTrade]=useState("");const[sites,setSites]=useState<Site[]>([]);const[tasks,setTasks]=useState<Task[]>([]);const[selectedSite,setSelectedSite]=useState("");const[message,setMessage]=useState("");async function loadRole(){const{data:userData}=await supabase.auth.getUser();if(!userData?.user){setMessage("Not logged in. Please login first.");return}const{data}=await supabase.from("profiles").select("role, trade").eq("id",userData.user.id).single();setRole(data?.role||"");setUserTrade(data?.trade||"")}async function loadSites(){const{data}=await supabase.from("sites").select("id, site_name").order("created_at",{ascending:false});setSites(data||[]);if(data&&data.length>0&&!selectedSite){setSelectedSite(data[0].id);loadTasks(data[0].id)}}async function loadTasks(siteId:string){setMessage("Loading visual programme...");const{data,error}=await supabase.from("programme_tasks").select("*").eq("site_id",siteId).order("start_date",{ascending:true});if(error){setMessage("Error: "+error.message);return}setTasks(data||[]);setMessage("Visual programme loaded")}function handleSiteChange(siteId:string){setSelectedSite(siteId);loadTasks(siteId)}useEffect(()=>{loadRole();loadSites()},[]);const visibleTasks=role==="subcontractor"&&userTrade?tasks.filter(t=>t.trade===userTrade):tasks;return <main><h1>Visual Programme Board</h1><div className="status-box">Status: {message}<br/>Role: {role||"Loading..."} {role==="subcontractor"?` | Trade: ${userTrade||"Not set"}`:""}</div><div className="card no-print"><h2>Select Site</h2><select value={selectedSite} onChange={e=>handleSiteChange(e.target.value)}>{sites.map(site=><option key={site.id} value={site.id}>{site.site_name}</option>)}</select></div><div className="card"><h2>Tasks by Plot</h2>{visibleTasks.length===0&&<p>No tasks to show.</p>}<div className="visual-grid">{visibleTasks.map(task=><div key={task.id} className="task-card"><h3 style={{marginTop:0}}>Plot {task.plot_number}</h3><p><strong>{task.task_name}</strong></p><p>Trade: {task.trade}</p><p>{task.start_date} → {task.end_date}</p><span className="badge" style={statusStyle(task.status)}>{task.status}</span><br/><br/><a href={`/suggested-edits?taskId=${task.id}`}>Suggest Change</a></div>)}</div></div></main>}
