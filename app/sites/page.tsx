"use client";import{useEffect,useState}from"react";import{supabase}from"../../lib/supabase";type Site={id:string;site_name:string;developer:string|null;status:string|null};export default function Sites(){const[sites,setSites]=useState<Site[]>([]);const[siteName,setSiteName]=useState("");const[developer,setDeveloper]=useState("");const[role,setRole]=useState("");const[message,setMessage]=useState("");async function loadRole(){const{data:userData}=await supabase.auth.getUser();if(!userData?.user){setMessage("Not logged in. Please login first.");return}const{data,error}=await supabase.from("profiles").select("role").eq("id",userData.user.id).single();if(error){setMessage("Role load error: "+error.message);return}setRole(data.role)}async function loadSites(){setMessage("Loading sites...");const{data,error}=await supabase.from("sites").select("id, site_name, developer, status").order("created_at",{ascending:false});if(error){setMessage("Load error: "+error.message);return}setSites(data||[]);setMessage("Sites loaded")}async function addSite(){if(role==="subcontractor"){setMessage("Subcontractors cannot create sites.");return}if(!siteName.trim()){setMessage("Please enter a site name");return}setMessage("Adding site...");const{error}=await supabase.from("sites").insert({site_name:siteName,developer,status:"active"});if(error){setMessage("Insert error: "+error.message);return}setSiteName("");setDeveloper("");setMessage("Site added successfully");loadSites()}useEffect(()=>{loadRole();loadSites()},[]);return <main><h1>Sites</h1><p>Create and manage your active developments.</p><div className="status-box">Status: {message}<br/>Role: {role||"Loading..."}</div>{role!=="subcontractor"&&<div className="card"><h2>Add New Site</h2><div style={{display:"flex",gap:12,flexWrap:"wrap"}}><input value={siteName} onChange={e=>setSiteName(e.target.value)} placeholder="Site name"/><input value={developer} onChange={e=>setDeveloper(e.target.value)} placeholder="Developer"/><button type="button" onClick={addSite}>Add Site</button></div></div>}{role==="subcontractor"&&<div className="card"><h2>Restricted Access</h2><p>Subcontractors can view invited programme tasks, but cannot create or manage sites.</p></div>}<div className="card"><h2>Saved Sites</h2>{sites.length===0&&<p>No sites yet.</p>}<div style={{display:"grid",gap:14}}>{sites.map(site=><div key={site.id} style={{border:"1px solid #edf1f5",borderRadius:14,padding:18,background:"#fafcff"}}><h3 style={{margin:"0 0 6px"}}>{site.site_name}</h3><p style={{margin:"0 0 6px"}}>Developer: {site.developer||"Not set"}</p><p style={{margin:"0 0 12px"}}>Status: {site.status}</p><a href="/programme"><button>View Programme</button></a></div>)}</div></div></main>}
