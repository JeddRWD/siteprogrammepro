"use client";
import { useEffect, useMemo, useState } from "react";
import { supabase } from "../../lib/supabase";
type Site={id:string;site_name:string}; type Task={id:string;plot_number:string|null;task_name:string|null;trade:string|null;start_date:string|null;end_date:string|null;status:string|null};
function daysBetween(a:Date,b:Date){return Math.max(0,Math.round((b.getTime()-a.getTime())/(1000*60*60*24)))}
export default function ProgrammeGantt(){const[role,setRole]=useState("");const[userTrade,setUserTrade]=useState("");const[sites,setSites]=useState<Site[]>([]);const[tasks,setTasks]=useState<Task[]>([]);const[selectedSite,setSelectedSite]=useState("");const[message,setMessage]=useState("");async function loadRole(){const{data:userData}=await supabase.auth.getUser();if(!userData?.user){setMessage("Not logged in. Please login first.");return}const{data}=await supabase.from("profiles").select("role, trade").eq("id",userData.user.id).single();setRole(data?.role||"");setUserTrade(data?.trade||"")}async function loadSites(){const{data}=await supabase.from("sites").select("id, site_name").order("created_at",{ascending:false});setSites(data||[]);if(data&&data.length>0&&!selectedSite){setSelectedSite(data[0].id);loadTasks(data[0].id)}}async function loadTasks(siteId:string){setMessage("Loading Gantt view...");const{data,error}=await supabase.from("programme_tasks").select("*").eq("site_id",siteId).order("start_date",{ascending:true});if(error){setMessage("Error: "+error.message);return}setTasks(data||[]);setMessage("Gantt view loaded")}function handleSiteChange(siteId:string){setSelectedSite(siteId);loadTasks(siteId)}useEffect(()=>{loadRole();loadSites()},[]);const visibleTasks=role==="subcontractor"&&userTrade?tasks.filter(t=>t.trade===userTrade):tasks;const datedTasks=visibleTasks.filter(t=>t.start_date&&t.end_date);const range=useMemo(()=>{if(datedTasks.length===0)return{start:new Date(),end:new Date(),days:1};const starts=datedTasks.map(t=>new Date(t.start_date as string));const ends=datedTasks.map(t=>new Date(t.end_date as string));const start=new Date(Math.min(...starts.map(d=>d.getTime())));const end=new Date(Math.max(...ends.map(d=>d.getTime())));return{start,end,days:Math.max(1,daysBetween(start,end)+1)}},[tasks,role,userTrade]);return <main><h1>Gantt Programme View</h1><div className="status-box">Status: {message}<br/>Role: {role||"Loading..."} {role==="subcontractor"?` | Trade: ${userTrade||"Not set"}`:""}</div><div className="card no-print"><h2>Select Site</h2><select value={selectedSite} onChange={e=>handleSiteChange(e.target.value)}>{sites.map(site=><option key={site.id} value={site.id}>{site.site_name}</option>)}</select></div><div className="card"><h2>Timeline</h2><p>{range.start.toLocaleDateString()} → {range.end.toLocaleDateString()}</p>{datedTasks.length===0&&<p>No dated tasks to show.</p>}<div className="gantt-wrap"><div className="gantt">{datedTasks.map(task=>{const start=new Date(task.start_date as string);const end=new Date(task.end_date as string);const offset=(daysBetween(range.start,start)/range.days)*100;const width=Math.max(4,((daysBetween(start,end)+1)/range.days)*100);return <div className="gantt-row" key={task.id}><div className="gantt-label">Plot {task.plot_number}<br/><small>{task.trade}</small></div><div className="gantt-track"><div className="gantt-bar" style={{left:`${offset}%`,width:`${width}%`}}>{task.task_name}</div></div></div>})}</div></div></div></main>}
