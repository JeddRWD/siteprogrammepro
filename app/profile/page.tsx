"use client";import{useEffect,useState}from"react";import{supabase}from"../../lib/supabase";export default function Profile(){const[user,setUser]=useState<any>(null);const[role,setRole]=useState("site_manager");const[trade,setTrade]=useState("");const[message,setMessage]=useState("Loading profile...");async function loadProfile(){const{data:userData}=await supabase.auth.getUser();if(!userData?.user){setMessage("No logged-in user found. Please login again.");return}setUser(userData.user);const{data,error}=await supabase.from("profiles").select("role, trade").eq("id",userData.user.id).single();if(error){setMessage("Profile load error: "+error.message);return}setRole(data.role||"site_manager");setTrade(data.trade||"");setMessage("Profile loaded")}async function updateProfile(){if(!user){setMessage("No user loaded. Please login again.");return}const{error}=await supabase.from("profiles").update({role,trade}).eq("id",user.id);if(error){setMessage("Update error: "+error.message);return}setMessage("Profile updated")}useEffect(()=>{loadProfile()},[]);return <main><h1>Profile</h1><div className="status-box">Status: {message}</div><div className="card"><h2>Your Role</h2><select value={role} onChange={e=>setRole(e.target.value)}><option value="site_manager">Site Manager</option><option value="subcontractor">Subcontractor</option><option value="contracts_manager">Contracts Manager</option></select><br/><br/><h2>Your Trade</h2><select value={trade} onChange={e=>setTrade(e.target.value)}><option value="">Not applicable</option><option value="Electrical">Electrical</option><option value="Plumbing">Plumbing</option><option value="Drylining">Drylining</option><option value="Joinery">Joinery</option><option value="Brickwork">Brickwork</option><option value="Roofing">Roofing</option><option value="Decorating">Decorating</option><option value="Groundworks">Groundworks</option></select><br/><br/><button type="button" onClick={updateProfile}>Save Profile</button></div></main>}
